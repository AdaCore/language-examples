project Observer is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Exec_Dir use ".";
   for Main use ("main.adb");

   package Pretty_Printer is
      for Default_Switches ("ada") use ("-M72", "-N");
   end Pretty_Printer;

end Observer;

